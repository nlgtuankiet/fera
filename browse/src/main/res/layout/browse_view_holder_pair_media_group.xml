<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  xmlns:tools="http://schemas.android.com/tools">

  <data>

    <variable
      name="leftText"
      type="String" />

    <variable
      name="rightText"
      type="String" />

    <variable
      name="leftImageSource"
      type="String" />

    <variable
      name="rightImageSource"
      type="String" />
  </data>

  <com.google.android.material.card.MaterialCardView
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:foreground="?attr/selectableItemBackground"
    app:cardCornerRadius="8dp"
    app:cardElevation="0dp"
    app:strokeColor="@color/media_group_stroke_color"
    app:strokeWidth="1dp">

    <androidx.constraintlayout.widget.ConstraintLayout
      android:layout_width="wrap_content"
      android:layout_height="wrap_content">

      <ImageView
        android:id="@+id/left_image"
        android:layout_width="64dp"
        android:layout_height="80dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:stringSource="@{leftImageSource}"
        tools:scaleType="centerCrop"
        tools:src="@tools:sample/avatars" />

      <View
        android:id="@+id/divider"
        android:layout_width="4dp"
        android:layout_height="4dp"
        app:layout_constraintStart_toEndOf="@id/left_image"
        app:layout_constraintTop_toTopOf="parent" />

      <ImageView
        android:id="@+id/right_image"
        android:layout_width="64dp"
        android:layout_height="80dp"
        app:layout_constraintLeft_toRightOf="@id/divider"
        app:layout_constraintTop_toTopOf="parent"
        app:stringSource="@{rightImageSource}"
        tools:scaleType="centerCrop"
        tools:src="@tools:sample/avatars" />

      <com.nlgtuankiet.fera.browse.view.LeftEllipsizePairTextView
        android:id="@+id/name_and_count"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="8dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/left_image"
        app:leftStyle="@style/Body1"
        app:leftText="@{leftText}"
        app:rightStyle="@style/Body1"
        app:rightText="@{rightText}" />

      <TextView
        android:id="@+id/type"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginTop="4dp"
        android:layout_marginEnd="8dp"
        android:layout_marginBottom="16dp"
        android:singleLine="true"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/name_and_count" />
    </androidx.constraintlayout.widget.ConstraintLayout>

  </com.google.android.material.card.MaterialCardView>
</layout>